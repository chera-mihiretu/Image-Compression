import 'package:flutter/material.dart';

class PickImage extends StatelessWidget {
  static const routeName = '/pick-image';
  const PickImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
